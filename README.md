This project demonstrates basic automation using PowerShell to simulate a real-world data intake workflow. The script prepares a structured workspace, generates a CSV file, displays its contents, and moves it through a processing lifecycle.
Features :Dynamic path handling using $HOME
Automated folder creation
CSV file generation
File content display
File movement between workflow stages
Completion confirmation message
Meaningful variable usage
